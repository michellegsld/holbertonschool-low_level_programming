#include "holberton.h"

/**
 * flip_bits - Finds how many bits needed to flip between two numbers
 * @n: The first number
 * @m: The second number
 */
unsigned int flip_bits(unsigned long int n, unsigned long int m)
{
	unsigned int total = 0;

	return (0);
}
